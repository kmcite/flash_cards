// import '../main.dart';

// @Entity()
// class Study {
//   @Id(assignable: true)
//   int id = 0;

//   String description = '';

//   @Property(type: PropertyType.date)
//   DateTime started = DateTime.now();
//   @Property(type: PropertyType.date)
//   DateTime ended = DateTime.now();
// }

// final studiesRepository = StudiesRepository();

// class StudiesRepository extends CRUD<Study> {}

// final saveStudy = (Study study) => studiesRepository.put(study);
// final deleteStudy = (Study study) => studiesRepository.remove(study.id);
